import { ComputeWateredDaysAgoPipe } from './compute-watered-days-ago-pipe';

describe('ComputeWateredDaysAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new ComputeWateredDaysAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
